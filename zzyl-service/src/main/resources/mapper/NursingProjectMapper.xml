<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.zzyl.mapper.NursingProjectMapper">

    <resultMap id="nursingProjectResultMap" type="com.zzyl.entity.NursingProject">
        <id column="id" property="id"/>
        <result column="name" property="name"/>
        <result column="order_no" property="orderNo"/>
        <result column="unit" property="unit"/>
        <result column="price" property="price"/>
        <result column="image" property="image"/>
        <result column="nursing_requirement" property="nursingRequirement"/>
        <result column="status" property="status"/>
        <result column="create_by" property="createBy"/>
        <result column="update_by" property="updateBy"/>
        <result column="remark" property="remark"/>
        <result column="create_time" property="createTime"/>
        <result column="update_time" property="updateTime"/>
        <result column="creator" property="creator"/>
    </resultMap>


    <!-- 分页查询护理项目 -->
    <select id="selectByPage" resultMap="nursingProjectResultMap">
        SELECT
            p.id, p.name, p.order_no, p.unit, p.price, p.image,
            p.nursing_requirement, p.status, p.create_by, p.update_by,
            p.remark, p.create_time, p.update_time,
            su.real_name AS creator
        FROM nursing_project p
        LEFT JOIN sys_user su ON p.create_by = su.id
        <where>
            <if test="name != null and name != ''">
                AND p.name LIKE CONCAT('%', #{name}, '%')
            </if>
            <if test="status != null">
                AND p.status = #{status}
            </if>
        </where>
        ORDER BY p.order_no ASC, p.create_time DESC
    </select>

    <!-- 新增护理项目 -->
    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO nursing_project (
            name, order_no, unit, price, image, nursing_requirement,
            status, create_by, update_by, remark, create_time, update_time
        ) VALUES (
            #{name}, #{orderNo}, #{unit}, #{price}, #{image}, #{nursingRequirement},
            #{status}, #{createBy}, #{updateBy}, #{remark}, #{createTime}, #{updateTime}
        )
    </insert>

    <!-- 根据ID查询护理项目 -->
    <select id="selectById" resultMap="nursingProjectResultMap">
        SELECT
            id, name, order_no, unit, price, image,
            nursing_requirement, status, create_by, update_by,
            remark, create_time, update_time
        FROM nursing_project
        WHERE id = #{id}
    </select>

    <!-- 更新护理项目 -->
    <update id="update">
        UPDATE nursing_project SET
            name = #{name},
            order_no = #{orderNo},
            unit = #{unit},
            price = #{price},
            image = #{image},
            nursing_requirement = #{nursingRequirement},
            status = #{status},
            update_by = #{updateBy},
            remark = #{remark},
            update_time = #{updateTime}
        WHERE id = #{id}
    </update>

    <!-- 删除护理项目 -->
    <delete id="deleteById">
        DELETE FROM nursing_project WHERE id = #{id}
    </delete>

    <!-- 根据状态查询护理项目列表 -->
    <select id="listByStatus" resultMap="nursingProjectResultMap">
        SELECT
            id, name, order_no, unit, price, image,
            nursing_requirement, status, create_by, update_by,
            remark, create_time, update_time
        FROM nursing_project
        WHERE status = #{status}
        ORDER BY order_no ASC, create_time DESC
    </select>

</mapper>